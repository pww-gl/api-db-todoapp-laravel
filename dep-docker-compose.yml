services:
  sqlite3:
    # build:
    #   context: .
    #   dockerfile: ./laravel/dev/Dockerfile
    #   args:
    #     UID: 1000
    #     GID: 1000
    #     UNAME: dan-wyn
    image: keinos/sqlite3
    container_name: api-db-sqlite3
    working_dir: /workdir
    user: 1000:1000
    volumes:
      - ./database:/workdir
    # ports:
    #   - "8000:8000" # for php artisan serve
    stdin_open: true
    tty: true
    # command: ['sqlite3']